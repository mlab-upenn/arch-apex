#ifndef VARIABLEREF__H
#define VARIABLEREF__H

#include "Expression.h"

/// class VariableRef - Reference to a variable, which could be in various scopes (sensors, vehicle, local in the program).
class VariableRef : public Expression {
};

#endif
