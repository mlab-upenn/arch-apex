#ifndef TRAFFICCONTROL__H
#define TRAFFICCONTROL__H

/// class TrafficControl - A static object such as a traffic light, a stop sign, etc. It is considered a point, so it has no length. A specific object type should derive from this class. A vanila StaticObject can be thought of as a mark point.
class TrafficControl {
};

#endif
